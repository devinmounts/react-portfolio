import React from 'react';
import './../styles/Bio.css';
import Card from '@material-ui/core/Card';
import CardContent from '@material-ui/core/CardContent';
import Typography from '@material-ui/core/Typography';
import Chip from '@material-ui/core/Chip';


class Bio extends React.Component {


  render(){
    let interestChips = [];
    let interests = ['JavaScript','React.js', 'React Native', 'Interactive Mobile Development', 'Angular.js', 'Consuming APIs', 'Building APIs', 'Digital Cartography', 'Data Visualization', 'Agile'];
       interestChips = interests.map((interest, index) => {
        return <Chip className='theme' color='primary'  key={index} label={interest} />
      });

    let skillsChips = [];
    let skills = ['Html', 'CSS', 'JavaScript', 'React.js', 'Redux', 'React Native', 'Firebase', 'Angular 4', 'RESTful routing', 'Google Maps API', 'Leaflet.js', 'MongoDB', 'D3.js', 'Git'];
    skillsChips = skills.map((skill, index) => {
     return <Chip className='theme' color='secondary'  key={index} label={skill} />
   });

    return(
      <div className='bio-style'>
        <header className='header'>
          About
        </header>
        <Card className='content-div'>
          <CardContent>
            <Typography variant='h3'>What I'm up to</Typography>
            <Typography variant='h5'>A recent grad of Epicodus, specializing in <strong>Javascript</strong> and <strong>React</strong>.  I am a passionate about growing as a front-end developer. I enjoy consuming APIs and managing state with React and Redux. When I'm not calling on APIs, creating realtime databases or exploring digital cartography, you can find me enjoying a grassy spot at the park, planting in the garden, and working in youth mentorship. I am inspired by projects such as <a href='https://www.native.io/industries/international_development'>these</a>.</Typography>
          </CardContent>
        </Card>
        <Card className='content-div'>
          <CardContent>
            <Typography variant='h3'>Previously</Typography>
            <Typography variant='h5'>I worked in Economic Development Research, as a recipient of Fulbright Association grant.
  My research analyzed the distribution and investment of royalties generated by hydroelectric energy production within the state of Minas Gerais, Brazil.  More recently, I worked in the field of community and youth development.  I was driven to enter into a career as a web engineer by my interest in data visualization with d3.js and of agile organizational methodologies </Typography>
          </CardContent>
        </Card>
        <Card className='content-div'>
          <CardContent>
            <Typography variant='h3'>Interests</Typography>
            {interestChips}
          </CardContent>
        </Card>
        <Card className='content-div'>
          <CardContent>
            <Typography variant='h3'>Skills</Typography>
            {skillsChips}
          </CardContent>
        </Card>
      </div>
    );
  }
}


export default Bio;
